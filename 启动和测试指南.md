# FreshID 启动和测试指南

## 📋 系统要求

### 后端要求
- Python 3.10+
- MySQL 8.0+
- 可用端口：8000

### 前端要求
- Node.js 16+
- npm 或 yarn
- 可用端口：3000

---

## 🚀 快速启动

### 1. 数据库准备

首先确保MySQL服务已启动，然后创建数据库：

```sql
CREATE DATABASE freshid CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 环境配置

在 `backend` 目录下创建 `.env` 文件（如果不存在）：

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的密码
DB_NAME=freshid

# 文件存储
UPLOAD_DIR=backend/uploads
BASE_URL=http://localhost:8000

# 日志
LOG_LEVEL=info
```

### 3. 启动后端服务

```bash
cd backend

# 安装依赖（如果还没安装）
pip install -r requirements.txt

# 启动服务
python main.py
```

后端启动成功后，你会看到：
```
数据库连接成功
表创建成功
模型加载成功
Uvicorn running on http://0.0.0.0:8000
```

### 4. 启动Vue管理员端

打开新的终端窗口：

```bash
cd vue

# 安装依赖（如果还没安装）
npm install

# 启动开发服务器
npm run dev
```

前端启动成功后，你会看到：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
```

---

## 🔑 登录信息

### 管理员账号
- **用户名**: `admin`
- **密码**: `admin123`

---

## 📊 功能测试清单

### 1. 登录功能测试
- [ ] 访问 http://localhost:3000
- [ ] 输入错误的用户名/密码，应该提示登录失败
- [ ] 输入正确的 admin/admin123，应该成功登录并跳转到首页

### 2. 数据统计页面测试
- [ ] 登录后自动跳转到数据统计页面
- [ ] 检查4个统计卡片是否正常显示
- [ ] 检查趋势图表是否正常加载
- [ ] 检查饼图是否正常显示
- [ ] 检查最近识别记录列表是否显示

### 3. 用户管理页面测试
- [ ] 点击左侧菜单的"用户管理"
- [ ] 检查用户列表是否正常显示
- [ ] 测试搜索功能（输入用户名搜索）
- [ ] 测试新增用户功能
- [ ] 测试编辑用户功能
- [ ] 测试删除用户功能

### 4. 识别记录页面测试
- [ ] 点击左侧菜单的"识别记录"
- [ ] 检查识别记录列表是否正常显示
- [ ] 测试按果蔬名称搜索
- [ ] 测试日期范围筛选
- [ ] 测试查看记录详情
- [ ] 测试删除单条记录
- [ ] 测试批量删除记录

### 5. 模型管理页面测试
- [ ] 点击左侧菜单的"模型管理"
- [ ] 检查模型列表是否正常显示
- [ ] 测试上传模型文件
- [ ] 测试激活模型
- [ ] 测试查看模型详情
- [ ] 测试删除模型

---

## 🔍 API测试

### 使用Swagger UI测试

后端启动后，可以访问：
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### 健康检查

```bash
curl http://localhost:8000/health
```

预期响应：
```json
{
  "status": "ok",
  "message": "服务运行正常",
  "version": "1.4.0",
  "database": "ok",
  "model": "ok",
  "checks": {
    "database": "ok",
    "model": "ok",
    "storage": "ok"
  }
}
```

### 管理员登录测试

```bash
curl -X POST http://localhost:8000/api/v1/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

预期响应：
```json
{
  "success": true,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "token_type": "bearer",
    "expires_in": 3600,
    "user": {
      "id": 1,
      "username": "admin",
      "nickname": "系统管理员"
    }
  },
  "message": "登录成功"
}
```

---

## 🐛 常见问题解决

### 问题1：数据库连接失败

**症状**：后端启动时显示 "数据库连接失败"

**解决方案**：
1. 检查MySQL服务是否启动
2. 检查 `.env` 文件中的数据库配置是否正确
3. 确认数据库 `freshid` 已创建
4. 检查防火墙设置

### 问题2：前端无法连接后端

**症状**：Vue页面显示网络错误

**解决方案**：
1. 确认后端服务已启动（访问 http://localhost:8000/health）
2. 检查 `vue/vite.config.js` 中的代理配置
3. 清除浏览器缓存

### 问题3：模型加载失败

**症状**：健康检查显示 model: "error"

**解决方案**：
1. 检查 `backend/models` 目录下是否有模型文件
2. 确认模型文件格式正确（.pt, .pth, .onnx, .tflite）
3. 检查 `.env` 中的 MODEL_PATH 配置

---

## 📝 开发提示

### 后端开发
- 代码热重载：修改Python文件后会自动重启
- 日志级别：在 `.env` 中设置 `LOG_LEVEL=debug` 查看详细日志
- API文档：访问 http://localhost:8000/docs 交互式测试API

### 前端开发
- 代码热重载：修改Vue文件后浏览器会自动刷新
- 开发工具：安装 Vue DevTools 浏览器扩展
- 网络请求：打开浏览器开发者工具查看Network标签

---

## 🎯 下一步

完成测试后，你可以：
1. 添加测试数据到数据库
2. 上传真实的YOLO模型文件
3. 接入真实的手机验证码服务
4. 配置HTTPS证书
5. 部署到生产环境

---

**文档版本**: V1.0  
**最后更新**: 2026-03-13
