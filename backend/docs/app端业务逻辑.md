# FreshID 果蔬识别 App 业务逻辑文档

## 文档说明

本文档详细描述 FreshID 果蔬识别 App 的完整业务逻辑，涵盖用户使用全流程、核心功能闭环、异常处理、数据流转及业务规则，适配移动端场景，可直接作为研发落地、测试校验、产品迭代的核心参考依据。文档兼顾用户体验与技术可行性，明确各环节逻辑规则、优先级及约束条件，确保团队对业务认知一致。

- **文档版本**：V1.0
- **适用范围**：FreshID 果蔬识别 App 研发、测试、产品设计及相关协作人员
- **核心目标**：明确 App 业务流程、数据流转、异常处理及规则约束，保障功能闭环、运行稳定，提升用户使用体验。

# 一、核心业务架构（分层设计）

App 业务架构采用分层设计，各层职责清晰、衔接顺畅，确保业务逻辑可扩展、可维护，具体分层及职责如下：

## 1.1 架构分层图

!\[img]\(理摸量 同用录 -erearaers (需高 识 (可)同云 腾化 睡 存 法用 校控 业 反 反操钱面互交 发用 贡首用))

## 1.2 各层核心职责

- 用户层：用户在 App 端的所有操作行为，包括首页浏览、拍照/相册选择识别、历史记录查看、设置调整、用户登录等，是业务流程的触发源头。
- 交互层：承接用户操作，负责页面跳转、操作反馈、权限申请与校验、弹窗提示等，确保用户操作与业务逻辑层的顺畅衔接，提升交互体验。
- 业务逻辑层：核心层，负责所有业务流程的控制、规则校验、异常判断与处理，协调交互层、数据层、算法层的协同工作，确保业务闭环。
- 数据层：负责数据的存储、读取、更新与同步，分为本地存储和云端存储，保障数据安全、可访问，支撑业务流程的正常运行。
- 算法层：负责图像识别相关的算法处理，包括图片预处理、识别模型调用、识别结果解析，是果蔬识别核心功能的技术支撑。
- 本地存储：采用 Room 数据库和 SharedPreferences，存储用户本地识别记录、用户信息、App 配置等数据，支持离线使用核心功能。
- 云端存储（可选）：存储登录用户的识别记录、收藏状态等数据，支持多设备同步，提升用户数据安全性与便捷性。
- 果蔬识别模型：包含本地轻量化 CNN 模型（适配离线场景）和云端识别 API（适配高精度场景），支撑果蔬识别功能的实现。

# 二、核心业务流程（按用户使用路径）

以下流程覆盖用户从 App 启动到功能使用、记录管理的全路径，明确各环节的触发条件、执行逻辑、分支判断及最终结果，确保流程闭环、无断点。

## 2.1 首次启动流程（冷启动）

### 2.1.1 流程示意图

!\[img]\(是一 请求权限(相机/相册/存储 引导页(3页:功能介绍一使 同意隐私协议 进入首页(功能受限) App启动 首次启动? 权限齐全 用户拒绝 否 检查权限状态 权限缺失 弹窗引导开启权限 用户同意 跳转系统设置页)

### 2.1.2 关键规则

- 权限优先级：相机 > 存储 > 相册。核心功能（拍照识别）依赖相机权限，无相机权限则隐藏“拍照识别”按钮，仅保留“相册选择”功能；无存储权限则无法保存识别记录；无相册权限则无法从相册选择图片识别。
- 引导页规则：仅首次启动 App 时显示，共 3 页，分别介绍 App 核心功能（果蔬识别）、使用教程（拍照/选图步骤）、隐私协议，支持“跳过”按钮，点击跳过可直接进入首页，但需后续弹窗确认隐私协议。
- 隐私协议规则：必须同意隐私协议才能使用核心识别功能，拒绝隐私协议则仅可浏览首页，无法触发拍照/相册识别操作，且弹窗提示“同意隐私协议即可使用全部功能”。
- 权限引导规则：非首次启动时，检查核心权限（相机、存储）状态，缺失则弹窗引导用户开启，用户同意则跳转至系统权限设置页，用户拒绝则进入首页，功能按权限缺失情况受限。

## 2.2 核心功能 - 果蔬识别流程（核心闭环）

### 2.2.1 流程示意图

!\[img]\(调用相机AP 拍摄图片 (名称/置信度/营 展示识别结果页 标记记录为"已收藏" 上传阴片至识别接口 调用相册AP 选择图片 回到首页 首页点击"图像 调用系统分享接口)

### 2.2.2 关键规则

#### （1）图片校验规则

- 尺寸校验：图片最小尺寸 480×480px，最大尺寸 2048×2048px，避免图片过大导致识别卡顿、过小影响识别精度。
- 清晰度校验：通过边缘检测算法对图片进行轻量化校验，过滤模糊、虚化严重的图片，确保识别模型能准确捕捉果蔬特征。
- 内容校验：初步过滤非果蔬类图片（如纯文字、人像、风景等），减少无效识别，提升用户体验。
- 校验失败处理：弹窗提示具体问题（如“图片模糊，请重新拍摄”“图片尺寸过小，请调整角度”），引导用户重新选择/拍摄，流程返回“选择识别方式”环节。

#### （2）识别方式选择规则

- 本地模型：内置轻量化 CNN 模型，支持 50+ 常见果蔬识别，适配离线场景，无需网络即可完成识别，识别速度较快（≤5s），精度满足日常使用需求。
- 云端 API：调用第三方视觉识别接口（如百度 AI、阿里云视觉），支持 200+ 果蔬识别（含稀有果蔬），精度更高，但依赖网络，识别速度受网络状态影响（≤10s）。
- 自动切换规则：默认优先使用本地模型，无网络时强制使用本地模型；有网络时，用户可手动切换至云端模型（在识别页提供“高精度识别”选项）；本地模型识别失败时，自动尝试云端识别（需网络）。

#### （3）识别结果解析规则

- 置信度≥80%：直接展示识别结果（果蔬名称、置信度），并关联完整营养数据、食用建议等信息。
- 60%≤置信度<80%：展示“疑似 XX（置信度 XX%）”，并推荐 2-3 种相似果蔬，供用户手动确认，确认后展示对应果蔬的详细信息。
- 置信度<60%：判定为“识别失败”，弹窗提示“未识别到果蔬，请调整图片角度、确保光线充足，或选择其他图片”，流程返回“选择识别方式”环节。

#### （4）记录保存规则

- 本地必存：识别成功后，自动保存识别记录至本地数据库，包括果蔬名称、置信度、图片路径（压缩后，≤200KB）、识别时间、营养数据、是否收藏等信息，支持离线查看。
- 云端可选：登录用户可开启“云端同步”功能，识别记录自动同步至云端；未登录用户（游客模式）仅本地存储，不进行云端同步。
- 图片存储：仅保存压缩后的缩略图用于历史记录展示，原图可由用户手动选择是否保存（默认不保存，避免占用过多存储空间）。

#### （5）用户操作规则

- 收藏：用户可在识别结果页点击“收藏”按钮，标记该记录为“已收藏”，收藏记录单独分组展示，支持后续快速查看。
- 再识别：点击“再识别一次”，直接跳转回图像识别页，保留当前识别方式（拍照/相册），方便用户快速重新识别。

## 2.3 历史记录管理流程

### 2.3.1 流程示意图

!\[img]\(滑动删除 弹窗确认《"是否删除该记录 确认 用户操作 清空全部 二次确认("是否清空所有历 一确认 清空所有本地记录 点击单条记录 展示该条识别详情页 加载本地记录列表(按时间 删除单条记录 用户操作 删除该记录 首页点击"历史记录" 是否有记录? 返回列表 无一 展示空状态(引导去识别 收藏/取消收藏 更新记录收藏状态)

### 2.3.2 关键规则

- 记录排序：默认按“识别时间倒序”展示（最新识别的记录排在最前面），支持用户手动切换排序方式（如“按果蔬名称正序/倒序”）。
- 空状态处理：无识别记录时，展示空状态页面，包含“空文件夹”图标、“暂无识别记录”标题、“快去试试图像识别吧！”描述，以及“去识别”按钮，点击按钮直接跳转至图像识别页。
- 删除规则：
  - 单条删除：滑动单条记录，显示“删除”按钮，点击后弹窗确认，确认后删除该条本地记录（云端同步记录同步删除，需登录且开启云端同步）。
  - 批量清空：点击页面右上角“清空”按钮，弹出二次确认对话框（防止误操作），确认后清空所有本地记录（云端同步记录同步清空）。
- 收藏管理：在历史记录列表或详情页，用户可点击“收藏/取消收藏”按钮，更新记录的收藏状态，收藏记录会在列表中单独分组（置顶展示），方便快速查找。
- 缓存策略：历史记录中的图片仅保存压缩后的缩略图，定期清理过期记录（默认超过 3 个月的记录自动清理，可在设置页调整清理周期），避免占用过多存储空间。

## 2.4 用户管理流程（UID 生成与管理）

### 2.4.1 流程示意图

!\[img]\(tmp/428082233090\_docxword\_media\_image5.jpg null)

### 2.4.2 关键规则

- 登录策略：支持“游客模式”和“登录模式”，游客模式无需登录即可使用核心识别功能，仅本地存储记录；登录模式仅支持手机号登录，登录后可开启云端同步，实现多设备数据同步。
- 手机号登录规则：输入手机号后，获取短信验证码（有效期 5 分钟），验证通过后完成登录，自动创建用户账号（生成专属 UID），默认昵称为“用户+手机号后 4 位”。
- 数据同步规则：登录后，自动拉取云端历史记录、收藏状态至本地，与本地记录合并（冲突时以“时间最新”为准）；后续识别记录、收藏状态变更，自动同步至云端。
- 用户信息管理：已登录用户可编辑头像（从相册选择/拍照）、昵称，保存后本地和云端同步更新；可查看“已识别果蔬数量”“收藏数量”等个人数据，并在 UI 中展示用户专属 UID。
- 退出与注销规则：
  - 退出登录：点击“退出登录”，清除本地登录状态，保留本地记录，后续使用切换为游客模式，不影响云端数据。
  - 注销账号：点击“注销账号”，弹出二次确认对话框（提示“注销后将删除所有云端数据，本地记录保留”），确认后删除云端用户数据，注销后切换为游客模式。
- 数据隔离：不同用户的记录完全隔离，本地仅保存当前登录用户（或游客）的记录，切换用户后，自动加载对应用户的记录。
- UID 创建规则：新用户注册时自动生成专属 UID，UID 为 8 位数字，从 00000001 开始顺序递增，直至 99999999（共 99999999 个可用 ID），确保 UID 生成算法的唯一性和连续性。

## 2.5 设置页业务流程

### 2.5.1 流程示意图

!\[img]\(语言设置 展示语言列表(简体/繁体/英 切换开关→保存状态→后续 自动保存历史 选择设置项 进入设置页 关于我们 展示版本/开发者/隐私政策 有更新 弹窗提示("发现v1.1.0,是 检查更新 请求版本接口→对比本地版本 无更新 提示"当前为最新版本")

### 2.5.2 关键规则

- 语言设置：支持简体中文、繁体中文、英文三种语言，选择后保存配置，重启 App 生效；默认语言为系统语言，若系统语言非支持语言，默认使用简体中文。
- 自动保存历史：默认开启，切换开关后保存配置，开启时识别成功自动保存记录，关闭时识别成功不保存记录（用户可手动保存）；配置状态同步至本地存储，重启 App 不丢失。
- 关于我们：展示 App 当前版本号、开发者信息、隐私政策链接、用户反馈入口，点击隐私政策链接可查看完整隐私协议，点击用户反馈可提交意见建议。
- 检查更新：点击“检查更新”，请求云端版本接口，对比本地版本与云端版本：
  - 有更新：弹窗提示更新信息（版本号、更新内容），提供“立即更新”和“稍后更新”选项，点击立即更新则下载安装包，下载完成后自动提示安装。
  - 无更新：弹窗提示“当前为最新版本”，1 秒后自动关闭，不影响用户操作。
- 其他设置（扩展）：可后续迭代添加“清理缓存”“通知设置”“识别精度设置”等选项，丰富设置功能。

# 三、异常处理逻辑（保障流程稳定性）

为避免 App 崩溃、用户操作卡顿，针对各核心流程中的异常场景，制定明确的处理逻辑和用户反馈，确保异常可感知、可恢复，提升用户体验。

| 异常场景       | 触发条件                            | 处理逻辑                                                        | 用户反馈                                                        |
| ---------- | ------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------- |
| 相机权限拒绝     | 用户首次/后续拒绝相机权限，或权限被系统收回          | 隐藏“拍照识别”按钮，仅保留“相册选择”功能；记录权限状态，每次进入图像识别页时检查，若权限仍缺失，引导用户开启    | 轻量弹窗（可关闭）：“开启相机权限可体验拍照识别，点击前往设置”；“拍照识别”按钮置灰，鼠标悬浮提示“需开启相机权限” |
| 图片识别超时     | 本地模型识别耗时＞5s，或云端 API 识别耗时＞10s    | 终止当前识别流程，释放系统资源；本地模型超时则自动尝试云端识别（需网络），云端 API 超时则切换至本地模型      | 弹窗提示：“识别超时，请检查网络状态或重新尝试”，提供“重新识别”按钮                         |
| 网络异常（云端识别） | 无网络连接、网络波动较大，或云端 API 不可用        | 自动切换至本地轻量化模型（若内置）；若无本地模型，则禁用识别功能，仅保留历史记录查看                  | 弹窗提示：“网络异常，已切换至本地识别（精度可能降低）”；无本地模型时提示 “网络异常，无法进行识别，请检查网络”   |
| 识别结果为空     | 图片中无果蔬、果蔬占比过小，或模型无法匹配到对应果蔬      | 终止识别流程，引导用户调整图片（重新拍摄/选择），展示常见果蔬示例（如番茄、黄瓜），辅助用户正确操作          | 弹窗提示：“未识别到果蔬，请确保果蔬占图片主体，光线充足，或选择其他图片”，附带 2-3 张常见果蔬示例图       |
| 本地存储满      | 识别记录、图片缓存占用存储空间＞1GB，或设备存储空间不足 | 禁止继续保存识别记录，自动清理过期记录（超过 3 个月）；提供 “清理缓存”入口，清理图片缓存和过期记录        | 弹窗提示：“存储空间不足，无法保存识别记录，请清理缓存”，提供“立即清理”按钮，点击跳转至缓存清理页          |
| 模型加载失败     | 本地识别模型文件损坏、缺失，或模型版本不兼容          | 自动尝试下载最新本地模型（需网络）；无网络则禁用本地识别，仅保留云端识别（需网络）；下载失败则提示用户重新安装 App | 弹窗提示：“模型加载失败，正在下载最新模型（需联网）”；下载失败提示“模型下载失败，请检查网络或重新安装 App”   |
| 登录失败       | 手机号验证码错误、网络异常    | 终止登录流程，提示用户排查问题（如核对验证码、检查网络）             | 弹窗提示具体原因：“验证码错误，请重新输入” “网络异常，请检查网络连接”             |
| 页面跳转失败     | 页面资源加载失败、内存不足，或页面不存在            | 返回上一页面，释放内存资源；若无法返回，则重启 App 至首页                             | 弹窗提示：“页面加载失败，请重试”，提供 “重新加载”和“返回首页”按钮                        |

# 四、数据流转逻辑

数据流转贯穿 App 所有业务流程，明确数据的存储、读取、更新、同步规则，确保数据安全、一致、可访问，支撑业务闭环。数据流转分为本地数据存储和云端数据流转两部分。

## 4.1 本地数据存储（核心，适配离线场景）

本地数据采用 Room 数据库（结构化数据）和 SharedPreferences（配置数据）存储，确保数据持久化，支持离线使用核心功能。

### 4.1.1 数据库设计（Room）

共设计 3 张核心数据表，分别存储识别记录、用户信息、App 配置，字段定义如下：

| 表名                         | 字段               | 类型      | 是否主键 | 说明                                                   |
| -------------------------- | ---------------- | ------- | ---- | ---------------------------------------------------- |
| recognition\_record（识别记录表） | id               | Int     | 是    | 主键，自增，唯一标识一条识别记录                                     |
| <br />                     | fruit\_veg\_name | String  | 否    | 果蔬名称（如“番茄”“黄瓜”）                                      |
| <br />                     | confidence       | Float   | 否    | 识别置信度（0-1，如 0.98 表示 98%）                             |
| <br />                     | image\_path      | String  | 否    | 图片本地存储路径（压缩后的缩略图）                                    |
| <br />                     | nutrition\_data  | String  | 否    | 营养数据，JSON 格式（如 {"热量":"18kcal","维生素C":"19mg"}）        |
| <br />                     | create\_time     | Long    | 否    | 识别时间，时间戳（毫秒级），用于排序和过期清理                              |
| <br />                     | is\_collected    | Boolean | 否    | 是否收藏，默认 false（未收藏）                                   |
| <br />                     | user\_id         | String  | 否    | 关联用户 UID，游客为“guest\_随机数”，登录用户为 8 位数字 UID                  |
| user\_info（用户信息表）          | uid              | String  | 是    | 主键，用户唯一标识，8 位数字，从 00000001 开始顺序递增，直至 99999999                  |
| <br />                     | phone\_number    | String  | 否    | 关联手机号，仅登录用户有值，游客为空                         |
| <br />                     | nickname         | String  | 否    | 用户昵称，默认“用户+手机号后 4 位”                         |
| <br />                     | avatar\_path     | String  | 否    | 用户头像本地存储路径，默认使用默认头像                                  |
| <br />                     | login\_type      | String  | 否    | 登录方式，可选值：phone（手机号）、guest（游客） |
| setting（配置表）               | key              | String  | 是    | 配置键，如“auto\_save”（自动保存历史）、“language”（语言）、“last\_uid”（最后分配的 UID）             |
| <br />                     | value            | String  | 否    | 配置值，如“true”“zh-CN”（简体中文）                             |

### 4.1.2 SharedPreferences 存储

用于存储轻量级配置数据，无需结构化存储，主要包括：

- App 首次启动标记（is\_first\_launch）：用于控制引导页的显示。
- 当前登录用户 UID（current\_user\_uid）：用于快速加载当前用户的记录和信息。
- 云端同步开关状态（cloud\_sync）：用于控制识别记录是否自动同步至云端。
- 最后一次检查更新时间（last\_check\_update）：用于避免频繁请求更新接口。
- 最后分配的 UID（last\_uid）：用于生成下一个用户 UID，确保 UID 的唯一性和连续性。

## 4.2 云端数据流转（可选，适配多端同步）

云端数据流转仅针对登录用户，用于实现多设备数据同步，确保用户在不同设备上使用 App 时，识别记录、收藏状态保持一致。

### 4.2.1 数据上传

- 触发时机：登录用户识别成功后、收藏状态变更后、用户信息修改后。
- 上传内容：识别记录（果蔬名称、置信度、营养数据、识别时间、是否收藏）、用户信息（昵称、头像）、收藏状态。
- 上传策略：WiFi 环境下自动上传；移动网络环境下，弹窗提示用户“是否上传数据（消耗流量）”，用户同意后上传。

### 4.2.2 数据下载

- 触发时机：用户登录后、App 启动时（登录状态）、手动点击“同步数据”按钮。
- 下载内容：云端存储的识别记录、收藏状态、用户信息。
- 下载策略：优先下载未同步的新记录，与本地记录合并，冲突时以“时间最新”为准（云端记录时间晚则覆盖本地，反之保留本地）。

### 4.2.3 冲突处理

当本地记录与云端记录发生冲突（如同一时间识别的同一种果蔬，置信度或收藏状态不同），处理规则如下：

- 识别记录冲突：以“识别时间”为准，时间较新的记录覆盖时间较旧的记录。
- 收藏状态冲突：以“最后修改时间”为准，最后修改的状态覆盖之前的状态。
- 用户信息冲突：以云端用户信息为准，覆盖本地用户信息（确保多设备用户信息一致）。

# 五、核心业务规则（不可变逻辑）

以下规则为 App 业务的核心约束，不可随意修改，确保功能稳定性、用户体验一致性及数据安全性，贯穿 App 全生命周期。

- 识别优先级规则：本地轻量化模型优先（离线可用），云端 API 兜底（高精度）；无网络时强制使用本地模型，有网络时支持用户手动切换；当用户选择云端模型且设备处于无网络状态时，需立即显示明确的提示信息（如“当前无网络连接，无法使用云端模型，已自动切换至本地模型”），并自动切换至本地模型运行模式。
- 数据安全规则：用户拍摄/选择的图片仅本地存储（或经用户同意后上传至云端），绝不私自采集、泄露用户图片及个人信息；云端数据采用加密存储，保障用户隐私。
- 性能规则：单张图片识别耗时≤8s（本地模型≤5s，云端模型≤10s）；App 冷启动时间≤3s，热启动时间≤1s；页面跳转耗时≤500ms，确保操作流畅。
- 兼容规则：支持 Android 8.0+、iOS 12.0+ 系统版本；适配不同分辨率屏幕（手机、平板），确保页面布局正常、功能可用。
- 降级规则：无网络时，禁用云端识别、数据同步功能，仅保留本地识别、本地历史记录查看等核心功能；无相机权限时，禁用拍照识别，仅保留相册选择识别；无存储权限时，禁用记录保存功能，仅保留识别功能。
- 记录保存规则：识别记录本地必存，云端可选；图片仅保存压缩后的缩略图，原图需用户手动选择保存；过期记录（默认 3 个月）自动清理，可在设置页调整清理周期。
- 用户权限规则：核心权限（相机、存储）缺失时，不影响 App 正常启动，但对应功能受限；隐私协议必须同意才能使用核心识别功能，拒绝则仅可浏览首页。

# 六、扩展业务逻辑（未来可迭代）

基于当前核心业务逻辑，预留扩展空间，后续可根据用户需求、市场反馈迭代新增功能，以下为推荐的扩展业务逻辑，可按优先级逐步落地。

## 6.1 果蔬百科功能

- 功能描述：在识别结果页关联“果蔬百科”模块，展示果蔬的详细信息，包括产地、成熟季节、储存方式、营养价值、常见做法等。
- 业务逻辑：识别成功后，根据果蔬名称，从本地数据库或云端接口拉取百科数据，展示在结果页下方，支持用户点击“查看更多”查看完整百科内容。

## 6.2 饮食推荐功能

- 功能描述：基于用户的识别记录，分析用户常识别的果蔬类型，推荐“果蔬搭配食谱”“每日摄入量建议”“健康饮食方案”。
- 业务逻辑：统计用户历史识别记录，分类整理果蔬类型，结合营养数据，推荐合理的搭配方式和摄入量，每日推送 1-2 条饮食建议，提升用户粘性。

## 6.3 批量识别功能

- 功能描述：支持用户从相册多选图片（最多 5 张），批量进行果蔬识别，生成“果蔬清单”，展示所有识别结果及汇总营养信息。
- 业务逻辑：用户选择多张图片后，依次进行图片校验、识别，识别完成后，汇总所有结果，生成清单，支持用户批量收藏、批量删除、分享清单。

## 6.4 会员体系功能

- 功能描述：新增会员体系，分为普通会员和付费会员，付费会员解锁高级功能，提升用户付费转化。
- 业务逻辑：普通会员可使用基础识别功能、历史记录管理等核心功能；付费会员解锁“高精度识别”“稀有果蔬识别”“无广告”“批量识别上限提升”等权益，支持按月/按年付费，会员到期后自动降级为普通会员。

## 6.5 多语言扩展功能

- 功能描述：在现有三种语言（简体/繁体/英文）基础上，新增日语、韩语、西班牙语等多语言支持，适配海外用户。
- 业务逻辑：新增语言包，用户可在设置页选择对应语言，重启 App 后生效；识别结果、营养数据、百科内容同步适配对应语言，确保内容准确性。

# 七、总结

本文档完整覆盖 FreshID 果蔬识别 App 的核心业务逻辑，明确了从用户启动 App 到功能使用、记录管理的全流程，界定了各环节的规则、异常处理及数据流转，确保业务闭环、运行稳定、用户体验良好。

## 核心优势

- 流程闭环：覆盖“启动→权限→识别→结果→记录→管理”全用户路径，无断点，满足用户核心需求。
- 异常兜底：每个关键业务节点都制定了明确的异常处理逻辑，避免 App 崩溃、用户操作卡顿，提升用户体验。
- 灵活扩展：基础逻辑满足 MVP 版本落地，扩展逻辑可按优先级迭代，兼顾初期开发成本与长期规划。
- 数据安全：明确数据存储、同步规则，保障用户隐私和数据安全，符合隐私政策要求。

本文档可作为研发、测试、产品设计的核心参考，后续业务迭代时，需基于本文档的核心规则，补充扩展功能的详细逻辑，确保团队对业务认知一致。
